fixtures:
  repositories:
    # apache:
    #   repo: 'git://github.com/puppetlabs/puppetlabs-apache.git'
    #   ref:  '1.0.1'
  forge_modules:
    stdlib:
      repo: 'puppetlabs-stdlib'
      ref:  '4.2.2'
    apt:
      repo: 'puppetlabs-apt'
      ref:  '2.0.1'
  symlinks:
    ck: "#{source_dir}"
