fixtures:
  repositories:
    # apache:
    #   repo: 'git://github.com/puppetlabs/puppetlabs-apache.git'
    #   ref:  '1.0.1'
  forge_modules:
    stdlib:
      repo: 'puppetlabs-stdlib'
      ref:  '4.2.2'
    apt:
      repo: 'puppetlabs-apt'
      ref:  '1.8.0'
    vcsrepo:
      repo: 'puppetlabs-vcsrepo'
      ref:  '1.3.0'
    gcc:
      repo: 'puppetlabs-gcc'
      ref:  '0.3.0'
  symlinks:
    ck: "#{source_dir}"
